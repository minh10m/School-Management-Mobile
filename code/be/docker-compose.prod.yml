version: '3.8'

services:
  api:
    build:
      context: ./School_Management.API
      dockerfile: Dockerfile
    container_name: school_management_api_prod
    ports:
      - "80:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always
